# /etc/nginx/sites-available/homepage.conf
server {
    listen 80;
    server_name homepage.pridebytes.io;

    # Redirect HTTP traffic to HTTPS
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name homepage.pridebytes.io;

    # SSL certificates managed by Certbot
    ssl_certificate /etc/letsencrypt/live/homepage.pridebytes.io/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/homepage.pridebytes.io/privkey.pem;

    # SSL settings for better security
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'TLS_AES_128_GCM_SHA256:TLS_AES_256_GCM_SHA384:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;

    # Proxy to the Homepage container running on port 3000
    location / {
        proxy_pass http://172.18.0.3:3000;  # Replace with your actual Homepage container IP or host
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
